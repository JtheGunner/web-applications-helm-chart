# =============================================================================
# Example: deploy a PHP app directly from a Git repo
# =============================================================================
# Clones the portfolio repo at pod start, runs composer install in a build
# image, and serves it with PHP-FPM + Nginx.
#
# Deploy: helm install portfolio charts/webapp -f examples/git-source-portfolio.yaml
#
# For a private repo, create the SSH secret first:
#   kubectl create secret generic portfolio-ssh \
#     --from-file=ssh-privatekey=$HOME/.ssh/id_ed25519 \
#     --from-file=known_hosts=$HOME/.ssh/known_hosts
# =============================================================================

php:
  enabled: true

  # Runtime image: PHP-FPM for request handling.
  # The official image does NOT include composer — that's why the build step
  # below runs in a separate composer image.
  image:
    repository: php
    tag: "8.3-fpm-alpine"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi

  app:
    source: git

    git:
      url: https://github.com/JtheGunner/portfolio
      ref: master           # branch, tag, or commit SHA
      depth: 1              # shallow clone is enough for branch/tag
      # For a private repo over SSH:
      # url: git@github.com:JtheGunner/portfolio.git
      # sshSecret:
      #   name: portfolio-ssh

    # composer install after cloning, in the official composer image.
    build:
      enabled: true
      image:
        repository: composer
        tag: "2-php8.3"
        pullPolicy: IfNotPresent
      command: "composer install --no-dev --optimize-autoloader --no-interaction"
      resources:
        limits:
          cpu: 1000m
          memory: 1Gi

  # Env vars for the app (override the .env from the repo).
  env:
    - name: APP_ENV
      value: production

  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      - host: portfolio.example.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: portfolio-tls
        hosts:
          - portfolio.example.com
