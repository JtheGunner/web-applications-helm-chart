{{- if and .Values.php.enabled .Values.php.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webapp.php.configMapName" . }}
  labels:
    {{- include "webapp.php.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.php.config | nindent 2 }}
{{- end }}
