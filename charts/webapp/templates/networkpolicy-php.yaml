{{- if and .Values.php.enabled .Values.php.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "webapp.fullname" . }}-php
  labels:
    {{- include "webapp.php.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "webapp.php.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    {{- if .Values.php.networkPolicy.ingress }}
    {{- toYaml .Values.php.networkPolicy.ingress | nindent 4 }}
    {{- else }}
    - {}
    {{- end }}
{{- end }}
