{{- if and .Values.nodejs.enabled .Values.nodejs.ingress.enabled }}
{{- $fullName := include "webapp.fullname" . -}}
{{- $svcPort := .Values.nodejs.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-nodejs
  labels:
    {{- include "webapp.nodejs.labels" . | nindent 4 }}
  {{- with .Values.nodejs.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.nodejs.ingress.className }}
  ingressClassName: {{ .Values.nodejs.ingress.className }}
  {{- end }}
  {{- if .Values.nodejs.ingress.tls }}
  tls:
    {{- range .Values.nodejs.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.nodejs.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-nodejs
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
